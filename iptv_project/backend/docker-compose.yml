version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=prod
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - ./output:/app/output
